// Copy this file to `config.js` and fill in your Supabase project values.
// `config.js` is gitignored; this template is committed.
//
// Find these in your Supabase project: Settings → API
//   - Project URL     → SUPABASE_URL
//   - anon public key → SUPABASE_ANON_KEY
//
// The anon key is safe to keep client-side — RLS controls what it can read.
// Habits are scoped per-login-password (see the LOGIN modal in the app).

window.HABBIT_CONFIG = {
  SUPABASE_URL:      "https://YOUR-PROJECT-REF.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY"
};
